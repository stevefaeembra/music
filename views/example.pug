html
  head
    title= title
  body
    ul
        - for (var x=0;x<12;x++) {
            if (notez.includes(x))
                li(class="played note_"+x)
                    = notenames[x]
            else
                li(class="silent note_"+x) 
                    = notenames[x]
        - }
